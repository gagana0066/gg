{
  "name": "gg-frontend",
  "version": "1.0.0",
  "description": "Simple frontend for GG todo",
  "scripts": {
    "start": "parcel public/index.html --port 3000",
    "build": "parcel build public/index.html --dist-dir build"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}